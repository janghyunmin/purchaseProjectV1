<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="item"
            type="run.piece.domain.refactoring.portfolio.model.PortfolioMarketInfoVo" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/tap_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        app:cardCornerRadius="30dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="@color/g300_EAECF0">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tap_name_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/g700_757983"
            android:text="@{item.priceTitle}"
            android:layout_gravity="center"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:maxLines="1"
            android:ellipsize="end"
            style="@style/title_2"/>

    </com.google.android.material.card.MaterialCardView>
</layout>